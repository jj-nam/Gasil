<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="org.joonzis.mapper.HeartMapper">
	<!-- 해당 게시물에 좋아요를 눌렀는지 판단  1: 누름 | 0: 안누름 -->
	<select id="get" parameterType="org.joonzis.domain.HeartVO" resultType="int">
		select count(*) from tbl_heart where bno = #{bno} and user_id = #{user_id}
	</select>

	<!--  -->
	<insert id="insert" parameterType="org.joonzis.domain.HeartVO">
		insert into tbl_heart(hno, bno, user_id)
		values(
			heart_seq.nextval,
			#{bno},
			#{user_id}
		)
	</insert>

	<!-- 좋아요 검색  -->
	<select id="searchLike" resultType="int">
		SELECT COUNT(*) FROM tbl_heart
		WHERE bno = #{bno} AND user_id = #{user_id}
	</select>
	
	<!-- 좋아요 삭제  --> 
	<delete id="deleteLike" parameterType="org.joonzis.domain.HeartVO">
		DELETE FROM tbl_heart
		WHERE bno = #{bno} AND user_id = #{user_id}	
	</delete>



<!-- 	<select id="findLike" resultType="int" parameterType="org.joonzis.domain.HeartVO">
		select count(*) from tbl_heart where bno = #{bno} and user_id = #{user_id}
	</select>


	<insert id="likeUp" parameterType="org.joonzis.domain.HeartVO">
		insert into tbl_heart(hno, bno, user_id, likeOrNo)
		values(
			heart_seq.nextval,
			#{bno},
			#{user_id},
			1
		) 
	</insert>
	<delete id="likeDown" parameterType="org.joonzis.domain.HeartVO">
		DELETE FROM tbl_heart
		WHERE bno = #{bno} AND user_id = #{user_id}	
	</delete>
-->

</mapper>